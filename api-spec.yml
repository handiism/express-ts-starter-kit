openapi: "3.0.0"
info:
  version: 1.0.0
  title: API Docs
  description: Simple API Documentation Using Open API Swagger
servers:
  - url: "{{ &url }}"
paths:
  /:
    get:
      responses:
        default:
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  ping:
                    type: string
                    description: pong
                    example: pong
